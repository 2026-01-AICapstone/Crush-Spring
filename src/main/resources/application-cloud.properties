# AWS 클라우드 배포용 — t3.medium (Spring + MySQL + Frontend)
# FastAPI는 별도 g4dn.xlarge에서 실행, 환경변수로 URL 주입
spring.datasource.url=jdbc:mysql://crush-mysql:3306/crush-db
spring.datasource.username=crush-db-user
spring.datasource.password=${MYSQL_PASSWORD}

fastapi.url=${FASTAPI_URL}
